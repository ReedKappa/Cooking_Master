<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/back_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:menu="@menu/backstack_toolbar"
        android:background="@color/home_toolbar"
        app:titleTextColor="@color/white"
        app:navigationIcon="@drawable/back_arrow"/>
    


    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/receipt_pager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/back_toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/text_background"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        android:padding="16dp"/>

    <ImageView
        android:id="@+id/button_previous"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:background="@drawable/button_circle"
        android:foreground="@drawable/button_previous_arrow"
        app:layout_constraintTop_toBottomOf="@id/receipt_pager"
        app:layout_constraintStart_toStartOf="@id/receipt_pager"
        android:layout_marginStart="15dp"
        android:layout_marginTop="15dp"/>

    <ImageView
        android:id="@+id/button_next"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:background="@drawable/button_circle"
        android:foreground="@drawable/button_next_arrow"
        app:layout_constraintTop_toBottomOf="@id/receipt_pager"
        app:layout_constraintEnd_toEndOf="@id/receipt_pager"
        android:layout_marginEnd="15dp"
        android:layout_marginTop="15dp"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/button_add_receipt"
        android:src="@drawable/plus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/home_toolbar"/>

</androidx.constraintlayout.widget.ConstraintLayout>